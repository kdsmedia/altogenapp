
"use client"

import WalletPage from "@/app/dashboard/wallet/page"

export default function SaldoPage() {
  // Directly reuse the WalletPage logic but within the new /saldo route
  return <WalletPage />
}
